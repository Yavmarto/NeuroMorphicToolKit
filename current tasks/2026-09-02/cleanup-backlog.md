# Cleanup Backlog — Codebase Cleanup Sprint (CEL-3, Phase 1)

Returned by CTO for CEO approval. No cleanup edits have been made — this is audit-only.

## Baseline (measured 2026-09-02, before any cleanup)

- `ruff check .` (whole repo): **2 issues** (both `E402`) in `scripts/verify_akida_package_manifest.py:41` and `workers/neurobench_runner/main.py:21`.
- `dart analyze nmtk/neuro_toolkit/`: **470 issues**. Breakdown: `duplicate_definition` 97, `argument_type_not_assignable` 95, `undefined_function` 92, `inference_failure_on_untyped_parameter` 84, `avoid_dynamic_calls` 37, `experiment_not_enabled` 24, `undefined_identifier` 22, `undefined_class` 8, `list_element_type_not_assignable` 5, `uri_does_not_exist` 4, others 2.
- `mypy --strict` (no repo-wide config; run per module): `neurocnl` 2320 errors/310 files, `Neurochip` 487/48, `Neurohub` 177/55, `Neurobench` 86/14, `suite_api` 63/15, `workers` 114/18, `Neurosense` 17/7 (cleanest).

Close-out (Phase 3) will be measured against these numbers.

## Backlog

| Priority | Area | File(s) | Issue | Owner | Done Means |
|---|---|---|---|---|---|
| P0 | Flutter/tests | `nmtk/neuro_toolkit/test/features/neurocnl/simulator_preflight_provider_test.dart` | 92 `undefined_function`/`undefined_identifier`/`uri_does_not_exist` analyzer errors: file imports `package:glados/src/*.dart` (property-based testing) but `glados` is not a declared dependency in `pubspec.yaml` (only present transitively in `pubspec.lock`). Test file does not compile. | Engineer | `dart analyze` reports 0 errors for this file; either `glados` is added to `pubspec.yaml` dev_dependencies and the test compiles+passes, or (if the test is abandoned) it is deleted with a comment in the PR explaining why, confirmed by re-running `flutter test test/features/neurocnl/simulator_preflight_provider_test.dart` green. This file is already analyzer-red today, so fixing it cannot break a currently-green test. |
| P0 | Flutter/tests | 7 test files incl. `test/server_connection_notifier_test.dart`, `test/quick_connect_workspace_handoff_test.dart`, `test/features/neurocnl/widgets/snn_workflow_stepper_test.dart`, `snn_mobile_workflow_stepper_test.dart`, `error_reporting_test.dart` | 97 `duplicate_definition` errors: two `_` wildcard params in the same callback (e.g. `(_, _) {}`) are rejected by the analyzer even though `pubspec.yaml` declares `sdk: '>=3.8.0'` and `.dart_tool/package_config.json` shows `languageVersion: 3.9`. Suspect stale/mismatched analyzer state rather than a real language issue — installed `dart --version` is 3.12.2, well above the wildcard-variables minimum. | Engineer | Root cause identified and written down (stale `.dart_tool` cache vs. genuine SDK mismatch vs. analysis_options override). Fix applied (rename one `_` per pair to a real identifier is the safe fallback if caching isn't the cause). `dart analyze` shows 0 `duplicate_definition` errors. Confirm with `flutter test` on the affected files that they still pass — they are currently analyzer-red so no green test is at risk. |
| P0 | Flutter/lib | `nmtk/neuro_toolkit/lib/features/neurobench/models/workspace_route_state.dart:49-51` | `experiment_not_enabled` errors (part of the 24-count bucket) for `null-aware-elements`, despite SDK constraint already satisfied. Likely the same stale-cache root cause as above. | Engineer | Run `flutter clean && flutter pub get`, re-run `dart analyze`; if the error persists after a clean pub get, escalate as a genuine SDK/tooling issue rather than a source bug. Done means 0 `experiment_not_enabled` errors remain. |
| P0 | Python/schemas | `Neurobench/neurobench/app/schemas/common.py:6`, `Neurochip/neurochip/app/schemas/health.py:6`, `Neurohub/neurohub/app/schemas/health.py:30`, `Neurosense/neurosense/app/schemas/runtime.py:6`, `neurocnl/neurosim/app/schemas/runtime.py:6` | Identical `HealthResponse{status: str}` Pydantic model independently redefined in 5 modules — a standards violation (should live in one shared contracts location) confirmed by direct file inspection, not a duplication guess. | Engineer | A single `HealthResponse` schema exists in one shared location importable by all 5 modules; the 5 per-module definitions are replaced with imports; each module's own test suite (`pytest`) still passes; `/health` endpoint responses are byte-identical to before (verified by existing integration test or a quick manual diff of the two module tests before/after). |
| P0 | Python/schemas | `Neurosense/neurosense/app/schemas/devices.py:4` vs `Neurosense/neurosense/contracts/device_contracts.py:55` | Two different `DeviceInfo` classes with **different field sets** share the same name within the same module — a correctness risk (wrong-shape object could be passed where the other is expected), not just cosmetic duplication. | Engineer | One of the two is renamed or merged so `DeviceInfo` resolves unambiguously; `grep -rn "DeviceInfo" Neurosense` shows all call sites correctly typed; `Neurosense` test suite stays green. |
| P0 | Docs | `Neurochip/README.md:26`, `Neurohub/README.md:23` | Both READMEs point to `neurocnl/frontend/lib/screens/...` as the live location of their UI. `neurocnl/frontend/` has no `lib/` and no `pubspec.yaml` — it's leftover local build artifacts; the actual package was removed from the `neurocnl` submodule in commit `53a1b966` ("remove hollow frontend/ package shell"). The real UI lives in `nmtk/neuro_toolkit/`. Docs point at a path that no longer exists in source control. | PM | Both README lines updated to reference the correct path under `nmtk/neuro_toolkit/lib/...` (or a relative description that doesn't hardcode a since-removed path); grep confirms no remaining `neurocnl/frontend` references in either file. |
| P0 | Docs | `Neurobench/AGENTS.md:420`, `Neurosense/AGENTS.md:385`, `Neurohub/AGENTS.md:459`, `Neurosim/AGENTS.md:322`, `Neurochip/AGENTS.md:392`, `neurocnl/AGENTS.md:1646` | Docs audit sweep (this pass, PM) found a live contradiction, not just stale text: root `AGENTS.md` and 2 module files give the dev backend as `moosebun2@192.168.2.90`; the other 6 module `AGENTS.md` files give the same "dev backend" role as `moosebun2@192.168.2.51`. **CEO ruling (2026-09-02): `192.168.2.90` is authoritative** (matches root `AGENTS.md` and the project `CLAUDE.md` deployment section) — the six `.2.51` files are wrong. No shared-source refactor (e.g. a new config doc) — that is new infrastructure, out of scope for a cleanup sprint; correct the value in place. | PM | The 6 listed files have `192.168.2.51` replaced with `192.168.2.90` (both the `REMOTE_HOST=` value and the prose IP mention); `grep -rln "192.168.2.51" --include=AGENTS.md .` returns empty; no new shared config file is introduced. |
| P0 | Docs | `Neurochip/frontend/`, `Neurosense/frontend/`, `Neurohub/frontend/` | None of the three legacy frontend directories carry a deprecated marker. All three have zero tracked source (`git ls-files` returns empty — only local build artifacts like `.idea/` and `.flutter-plugins-dependencies`), so there is no in-directory file to annotate; the marker has to live in the parent module's README instead. | PM | One-line "deprecated, superseded by `nmtk/neuro_toolkit/`" note added to `Neurochip/README.md`, `Neurosense/README.md`, and `Neurohub/README.md`; `grep -l "deprecated" Neurochip/README.md Neurosense/README.md Neurohub/README.md` returns all three. No files are deleted. |
| P1 | Flutter/lib | 23 files incl. `setup_step.dart` (6), `neurohub_workspace_save.dart` (6), `akida_setup_pane.dart` (5) — full list from `grep -rn "OutlinedButton" nmtk/neuro_toolkit/lib --include=*.dart` | ~50 raw `OutlinedButton(` instances not using Zeta equivalents. | Designer | All `OutlinedButton` usages in `lib/` replaced with the Zeta equivalent component; `grep -c "OutlinedButton(" lib -r` returns 0 outside `ui_core` token/wrapper files; existing widget tests for touched screens stay green. |
| P1 | Flutter/lib | `custom_node_editor_panel.dart`, `setup_step.dart` (TextButton); `setup_step.dart` (Checkbox); `pipeline_node_property_panel.dart`, `property_panel.dart`, `simulator_panel.dart` (Switch); 7 files with raw `AppBar(`; `mobile_scaffold.dart` (FAB) | Remaining raw Material widget categories (TextButton, Checkbox, Switch, AppBar, FloatingActionButton) not using Zeta equivalents. | Designer | Each listed widget replaced with its Zeta equivalent; `grep` for the raw widget name in `lib/` (excluding `ui_core` wrapper/token files) returns 0; affected screens' widget tests stay green. |
| P1 | Flutter/lib | 32 files, 68 `Colors.*` hits + 11 files, 197 `Color(0x...)` hex-literal hits (excluding the 3 legitimate token-source files `ui_core/shell_tokens.dart`, `ui_core/zeta_theme.dart`, `ui_core/app_theme.dart`) | Hardcoded colors instead of `Zeta.of(context).colors`. Heaviest concentration in `features/neurocnl/widgets/canvas/*`. | Designer | All non-token-file `Colors.*` / hardcoded hex `Color(0x...)` usages replaced with `Zeta.of(context).colors.*`; `grep -rn "Colors\.\|Color(0x"` over `lib/` excluding the 3 token files returns 0; theme/dark-mode manual smoke check unaffected. |
| P1 | Flutter/lib | `features/neurocnl/widgets/cnl_editor.dart` (7 instances), plus `codegen_setup_pane.dart`, `nengo_param_chip.dart`, `run_step.dart`, `notebook_step.dart`, `export_screen.dart`, `network_canvas.dart`, `active_jobs_bar.dart`, `nir_importer_tab.dart`, `generated_code_pane.dart`, `top_app_bar.dart` — 26 hits/12 files total | Manual `TextStyle(...)` definitions instead of `ZetaTextStyles`. | Designer | All 26 manual `TextStyle(` instances replaced with `ZetaTextStyles` equivalents (or a documented exception where Zeta has no matching style); `grep -c "TextStyle(" lib -r` reduced to only the documented exceptions; visual smoke check of `cnl_editor.dart` (heaviest file) confirms no regression. |
| P1 | Flutter/lib | `features/neurobench/screens/` — `benchmark_screen.dart`, `comparison_screen.dart`, `regression_trends_screen.dart`, `report_screen.dart`, `robustness_screen.dart` (all 5 files); `features/neurocnl/screens/analysis_screen.dart`; `features/neurocnl/screens/hardware_screen.dart` | Dead screens: `grep -rn "\bClassName\b" lib` for each shows zero references outside the file's own declaration (confirmed per-class, not assumed). Likely superseded by `neurobench/screens/workbench_shell.dart` routing. | Engineer | Each file's class confirmed zero-caller by fresh `grep -rn` immediately before deletion (routes/nav can change between audit and execution); files deleted; `flutter analyze` and `flutter test` stay green; if any file turns out to have gained a caller since the audit, it is kept and this row is closed as "no longer applicable" instead of deleted. |
| P1 | Flutter/lib | `providers/canvas/canvas_selectors.dart` (`canvasGraphTopologyProvider`, `canvasSelectedEdgeIdProvider`, `canvasNodeIdsProvider`, `canvasNodeProvider`, `canvasEdgeProvider` — all 5 providers in the file), plus `mujocoAvailableProvider`, `activeWorkspaceSpecProvider`, `deployTargetsProvider`, `hasImportedCnlSpecProvider`, `categoriesProvider`, `canvasGraphProvider` | 11 Riverpod providers confirmed zero-reference beyond their own definition line (per-provider grep evidence). | Engineer | Fresh `grep -rn "<providerName>"` re-confirmed immediately before deletion (11 checks); unused providers removed; `flutter analyze` clean of unused-import warnings from the removal; existing tests stay green. |
| P1 | Python/routes | `/health` endpoint duplicated near-identically across `Neurobench/neurobench/app/main.py:195`, `Neurosense/neurosense/app/main.py:156`, `Neurohub/neurohub/app/main.py:174`, `Neurochip/neurochip/app/main.py:143`, `neurocnl/backend/app/main.py:263` | Same root cause as the `HealthResponse` schema duplication (P0 item above) — once a shared schema location exists, consolidate the handler too. | Engineer | Depends on the P0 `HealthResponse` item landing first. All 5 `/health` handlers call a shared helper (or the duplication is at minimum documented as intentional per-module isolation if consolidation is rejected); each module's health-check test still passes. |
| P1 | Python/schemas | `MessageResponse` (Neurobench/Neurochip), `StatusResponse` (Neurochip x2), `ExportRequest` (Neurosense/neurocnl x2), `ValidateRequest`/`ValidateResponse` (neurocnl backend/neurosim), `PreflightRequest`/`Response` (neurocnl backend x2), `CreateProjectRequest` (neurocnl neurosim x2), `VerifyRequest` (Neurochip x2) | Secondary tier of duplicate Pydantic schema names/shapes, lower blast-radius than `HealthResponse`/`DeviceInfo`. | Engineer | Each pair reviewed; genuinely identical shapes are consolidated into a shared location, genuinely different shapes sharing a name are renamed to avoid confusion; module test suites stay green. |
| P1 | Flutter/lib | `ui_core/widgets/log_viewer.dart:111`, `features/neurocnl/widgets/canvas/network_canvas.dart:1919` | Hardcoded `BorderRadius.circular(8)` / `.circular(6)` instead of sourcing from `NmtkShellTokens`/`NmtkDesignTokens`. | Designer | Both call sites use a token-sourced radius constant; `grep -n "BorderRadius.circular([0-9]" lib -r` shows only the 4 legitimate token-definition lines in `ui_core/app_theme.dart`. |
| P1 | Python/typing | `Neurosense/` (17 mypy --strict errors/7 files — cleanest of the 7 modules measured) | `mypy --strict` baseline is 3,264 errors across 7 modules (`neurocnl` alone at 2,320); fixing all of it is not cleanup-sprint-sized. Decision: scope this sprint's mypy work to the cleanest module only, and explicitly defer the rest. | Engineer | `Neurosense` reaches 0 `mypy --strict` errors. **CEO ruling (2026-09-02):** the CI gate is permitted as the one addition to this sprint, but it must be a single added `mypy --strict` invocation line inside `Neurosense`'s existing test step (e.g. its existing pytest/CI script) — no new pipeline, workflow file, or config layer. If keeping it that minimal isn't possible, drop the gate and just take the 17 errors to zero. The other 6 modules (`neurocnl`, `Neurochip`, `Neurohub`, `Neurobench`, `suite_api`, `workers`) are explicitly out of scope for this sprint — tracked as a separate future initiative, not silently dropped. |
| P2 | Python | `scripts/verify_akida_package_manifest.py:41`, `workers/neurobench_runner/main.py:21` | The 2 `E402` ruff violations — both are deliberate (`sys.path` setup before import; reuse of sibling app instance), not real bugs. | Engineer | Add per-file-ignore entries in `pyproject.toml` `[tool.ruff.lint.per-file-ignores]` matching the existing pattern already used for `workers/neurosense_hw/main.py`; `ruff check .` reports 0 issues. |
| P2 | Python | `Neurohub/neurohub/db/models.py:89` | Stale `# TODO` claims `SuiteConfigDB` "may be dead code" — verified false; it's actively used in `Neurohub/neurohub/app/services/config_service.py:18,42,52`. | Engineer | TODO comment removed (class is confirmed live, not a candidate for deletion); no behavior change. |
| P2 | Python | `scripts/backend_endpoint_smoke.py:27+` | ~30 lines of hardcoded NeuroCNL domain-spec string (`NEUROCNL_SMOKE_SPEC`) — product/domain content living in a CLI script rather than loaded from an owning-module fixture. | Engineer | Spec string moved to a fixture file under `neurocnl/` (or its existing test fixtures) and imported/loaded by the script; smoke script behavior unchanged, confirmed by re-running it. |
| P2 | Flutter/lib | 18 files, ~27 `Scaffold(` instances (heaviest: `tool_view.dart` 4, `app_router.dart` 3, `neurosim_app.dart` 2) | Raw `Scaffold` usage — lowest priority since `ui_core/widgets/*_scaffold.dart` wrapper components may already cover the intended Zeta shell pattern; needs a design call on whether raw `Scaffold` at the top-level app/router layer is acceptable Flutter-framework usage vs. a Zeta violation. | Designer | Designer makes and documents the call (raw `Scaffold` at root/router layer is acceptable framework plumbing vs. must wrap in Zeta shell); any flagged files are converted; decision + rationale recorded in the PR description. |

## Legacy frontends

Per sprint rule: `Neurochip/frontend/`, `Neurosense/frontend/`, `Neurohub/frontend/` are **not deleted**. All three are already effectively empty (no tracked source, git history shows removal). Marking them deprecated is now its own owned row above (P0/Docs) rather than a flagged aside — the note lands in the parent module's README since the directories themselves have no tracked file to hold it.

## Investigated — no action required

- **11 basenames initially flagged as possible duplicates** (`api_client.dart`, `app.dart`, `app_theme.dart`, `backend_support_banner.dart`, `cross_module_navigation.dart`, `export_dialog.dart`, `feature_launch_provider.dart`, `shell_adapter.dart`, `support.dart` x18, `workspace_feature.dart`, `workspace_file.dart`) — diffed all pairs/groups; every one has divergent content and is legitimately scoped per-feature. Two naming-collision risks worth a lightweight rename for clarity, not correctness: `ui_core/app_theme.dart` (holds `NmtkDesignTokens`) vs `neurocnl/theme/app_theme.dart` (unrelated), and `screens/tool_view/cross_module_navigation.dart` (helper) vs `services/cross_module_navigation.dart` (the class it calls). Optional low-effort follow-up: rename for clarity, not tracked as a backlog row since it's cosmetic only.

## Gate self-check

1. Owners: every row names Engineer, Designer, or PM — never "team". ✅
2. Every row has a "Done Means" statement. ✅
3. P0 rows (correctness/standards violations: broken test compilation, duplicate-definition analyzer errors, ambiguous `DeviceInfo` schema, stale doc path, dev-host IP contradiction, missing deprecation marker) are listed first. ✅
4. No "delete" recommendation lacks grep evidence — dead-screen and unused-provider rows cite the exact grep confirming zero external references, and each Done Means requires re-confirming with a fresh grep immediately before deletion (routes/callers can change between audit and execution). ✅
5. Legacy frontends are explicitly marked deprecated-not-deleted via an owned P0/Docs row (README note, since the directories hold no trackable file) — no row proposes deleting them. ✅
6. No row breaks a currently-green test without a plan: the two P0 Flutter test rows target files that are already analyzer-red today (cannot regress a green test); the new `Neurosense` mypy row targets a module with no existing strict-mode CI gate to break; every other row's Done Means requires the relevant test suite to stay green. ✅
7. Docs audit is complete, not deferred: the `AGENTS.md` sweep across root + 8 submodules ran this pass, and its one real finding (dev-host IP conflict) is now an owned P0 row instead of a placeholder. ✅
