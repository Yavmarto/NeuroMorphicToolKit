# requirements-verify.txt
# Environment for running CDD+PBT local verification (bash run.sh --step 5)
#
# Create and activate (from docs/unified-dev-pipeline/):
#   python3.11 -m venv .venv-verify
#   source .venv-verify/bin/activate
#   pip install -r requirements-verify.txt

# ── Module installs (editable) ───────────────────────────────────────────────
-e ../../neurocnl/.
-e ../../Neuro-Dream-Hand/.
-e ../../Neurobench/neurobench/.
-e ../../Neurochip/neurochip/.
-e ../../Neurohub/.
-e ../../Neurosense/.
-e ../../Neurosim/.

# ── Core contract + PBT dependencies ────────────────────────────────────────
pydantic>=2.0
hypothesis>=6.100.0
hypothesis[numpy]           # hypothesis.extra.numpy used in some property tests
numpy>=1.24.0               # used in some property tests

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-hypothesis            # pytest integration for hypothesis settings
pytest-timeout               # avoid runaway property tests

# ── Linting + type checking (used by verify script) ─────────────────────────
ruff>=0.4.0
mypy>=1.8.0

# ── Type stubs ───────────────────────────────────────────────────────────────
types-requests
