name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

defaults:
  run:
    shell: bash

permissions:
  contents: read
  pull-requests: read

jobs:
  detect-changes:
    runs-on: [self-hosted, Linux, X64]
    permissions:
      contents: read
      pull-requests: read
    outputs:
      neurosim: ${{ steps.filter.outputs.neurosim }}
      neurochip: ${{ steps.filter.outputs.neurochip }}
      neurochip_frontend: ${{ steps.filter.outputs.neurochip_frontend }}
      neurosense: ${{ steps.filter.outputs.neurosense }}
      neurosense_frontend: ${{ steps.filter.outputs.neurosense_frontend }}
      neurohub: ${{ steps.filter.outputs.neurohub }}
      neurohub_frontend: ${{ steps.filter.outputs.neurohub_frontend }}
      neurodreamhand: ${{ steps.filter.outputs.neurodreamhand }}
      neurocnl: ${{ steps.filter.outputs.neurocnl }}
      neurocnl_frontend: ${{ steps.filter.outputs.neurocnl_frontend }}
      neurobench: ${{ steps.filter.outputs.neurobench }}
      neurobench_frontend: ${{ steps.filter.outputs.neurobench_frontend }}
      neurotoolkit: ${{ steps.filter.outputs.neurotoolkit }}
      nmtk_ui_core: ${{ steps.filter.outputs.nmtk_ui_core }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0
      - name: Update submodules safely
        run: git submodule update --init || true
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            neurosim:
              - 'Neurosim/**'
            neurochip:
              - 'Neurochip/neurochip/**'
            neurochip_frontend:
              - 'Neurochip/frontend/**'
              - 'nmtk_ui_core/**'
            neurosense:
              - 'Neurosense/neurosense/**'
            neurosense_frontend:
              - 'Neurosense/frontend/**'
              - 'nmtk_ui_core/**'
            neurohub:
              - 'Neurohub/neurohub/**'
            neurohub_frontend:
              - 'Neurohub/frontend/**'
              - 'nmtk_ui_core/**'
            neurodreamhand:
              - 'Neuro-Dream-Hand/**'
            neurocnl:
              - 'neurocnl/**'
              - '!neurocnl/frontend/**'
            neurocnl_frontend:
              - 'neurocnl/frontend/**'
              - 'nmtk_ui_core/**'
            neurobench:
              - 'Neurobench/neurobench/**'
            neurobench_frontend:
              - 'Neurobench/frontend/**'
              - 'nmtk_ui_core/**'
            neurotoolkit:
              - 'nmtk/neuro_toolkit/**'
              - 'nmtk_ui_core/**'
            nmtk_ui_core:
              - 'nmtk_ui_core/**'

  pre-commit:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          which python || true
          which pytest || true
      - name: Install pre-commit
        run: python -m pip install pre-commit
      - name: Run pre-commit
        run: pre-commit run --all-files

  test-neurosim:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurosim == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - name: Configure tool cache
        shell: bash
        run: |
          TOOL_CACHE="${RUNNER_TEMP}/_tool_cache"
          mkdir -p "$TOOL_CACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOL_CACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOL_CACHE" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Update submodule
        run: git submodule update --init Neurosim || true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        env:
          AGENT_TOOLSDIRECTORY: ${{ runner.temp }}/_tool_cache
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          if [ "$RUNNER_OS" = "Windows" ]; then
            where python || true
            where pytest || true
          else
            which python || true
            which pytest || true
          fi
      - name: Install dependencies
        run: |
          if [ -d "Neurosim" ] && [ -f "Neurosim/pyproject.toml" ]; then
            python -m pip install -e ./Neurosim
            python -m pip install pytest httpx ruff mypy
          else
            echo "Submodule missing, skipping"
          fi
      - name: Lint with ruff
        run: |
          if [ -d "Neurosim" ] && [ -f "Neurosim/pyproject.toml" ]; then
            cd Neurosim
            python -m ruff check .
            python -m ruff format --check .
          fi
      - name: Type check with mypy
        run: |
          if [ -d "Neurosim" ] && [ -f "Neurosim/pyproject.toml" ]; then
            cd Neurosim
            python -m mypy .
          fi
      - name: Run tests
        run: |
          if [ -d "Neurosim" ] && [ -f "Neurosim/pyproject.toml" ]; then
            cd Neurosim
            python -m pytest --tb=short --hypothesis-show-statistics
          fi

  test-neurochip:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurochip == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - name: Configure tool cache
        shell: bash
        run: |
          TOOL_CACHE="${RUNNER_TEMP}/_tool_cache"
          mkdir -p "$TOOL_CACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOL_CACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOL_CACHE" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Update submodule
        run: git submodule update --init Neurochip || true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
        env:
          AGENT_TOOLSDIRECTORY: ${{ runner.temp }}/_tool_cache
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          if [ "$RUNNER_OS" = "Windows" ]; then
            where python || true
            where pytest || true
          else
            which python || true
            which pytest || true
          fi
      - name: Install dependencies
        run: |
          if [ -d "Neurochip/neurochip" ] && [ -f "Neurochip/neurochip/pyproject.toml" ]; then
            python -m pip install poetry
            cd Neurochip/neurochip
            poetry install
          else
            echo "Submodule missing, skipping"
          fi
      - name: Lint with ruff
        run: |
          if [ -d "Neurochip/neurochip" ] && [ -f "Neurochip/neurochip/pyproject.toml" ]; then
            cd Neurochip/neurochip
            poetry run ruff check .
            poetry run ruff format --check .
          fi
        continue-on-error: true # Allow for now if not configured
      - name: Type check with mypy
        run: |
          if [ -d "Neurochip/neurochip" ] && [ -f "Neurochip/neurochip/pyproject.toml" ]; then
            cd Neurochip/neurochip
            poetry run mypy .
          fi
      - name: Run tests
        run: |
          if [ -d "Neurochip/neurochip" ] && [ -f "Neurochip/neurochip/pyproject.toml" ]; then
            cd Neurochip/neurochip
            poetry run pytest --tb=short --hypothesis-show-statistics
          fi

  test-neurosense:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurosense == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - name: Configure tool cache
        shell: bash
        run: |
          TOOL_CACHE="${RUNNER_TEMP}/_tool_cache"
          mkdir -p "$TOOL_CACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOL_CACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOL_CACHE" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Update submodule
        run: git submodule update --init Neurosense || true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        env:
          AGENT_TOOLSDIRECTORY: ${{ runner.temp }}/_tool_cache
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          if [ "$RUNNER_OS" = "Windows" ]; then
            where python || true
            where pytest || true
          else
            which python || true
            which pytest || true
          fi
      - name: Install dependencies
        run: |
          if [ -d "Neurosense/neurosense" ] && [ -f "Neurosense/neurosense/pyproject.toml" ]; then
            python -m pip install -e "./Neurosense/neurosense[dev]"
            python -m pip install ruff mypy
          else
            echo "Submodule missing, skipping"
          fi
      - name: Lint with ruff
        run: |
          if [ -d "Neurosense/neurosense" ] && [ -f "Neurosense/neurosense/pyproject.toml" ]; then
            cd Neurosense/neurosense
            python -m ruff check .
            python -m ruff format --check .
          fi
      - name: Type check with mypy
        run: |
          if [ -d "Neurosense/neurosense" ] && [ -f "Neurosense/neurosense/pyproject.toml" ]; then
            cd Neurosense/neurosense
            python -m mypy .
          fi
      - name: Run tests
        run: |
          if [ -d "Neurosense/neurosense" ] && [ -f "Neurosense/neurosense/pyproject.toml" ]; then
            cd Neurosense/neurosense
            python -m pytest --tb=short --hypothesis-show-statistics
          fi

  test-neurohub:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurohub == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - name: Configure tool cache
        shell: bash
        run: |
          TOOL_CACHE="${RUNNER_TEMP}/_tool_cache"
          mkdir -p "$TOOL_CACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOL_CACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOL_CACHE" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Update submodule
        run: git submodule update --init Neurohub || true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        env:
          AGENT_TOOLSDIRECTORY: ${{ runner.temp }}/_tool_cache
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          if [ "$RUNNER_OS" = "Windows" ]; then
            where python || true
            where pytest || true
          else
            which python || true
            which pytest || true
          fi
      - name: Install dependencies
        run: |
          if [ -d "Neurohub/neurohub" ] && [ -f "Neurohub/neurohub/pyproject.toml" ]; then
            python -m pip install -e ./Neurohub/neurohub
            python -m pip install pytest httpx ruff mypy
          else
            echo "Submodule missing, skipping"
          fi
      - name: Lint with ruff
        run: |
          if [ -d "Neurohub/neurohub" ] && [ -f "Neurohub/neurohub/pyproject.toml" ]; then
            cd Neurohub/neurohub
            python -m ruff check .
            python -m ruff format --check .
          fi
      - name: Type check with mypy
        run: |
          if [ -d "Neurohub/neurohub" ] && [ -f "Neurohub/neurohub/pyproject.toml" ]; then
            cd Neurohub/neurohub
            python -m mypy .
          fi
      - name: Run tests
        run: |
          if [ -d "Neurohub/neurohub" ] && [ -f "Neurohub/neurohub/pyproject.toml" ]; then
            cd Neurohub/neurohub
            python -m pytest --tb=short --hypothesis-show-statistics
          fi

  test-neurodreamhand:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurodreamhand == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - name: Configure tool cache
        shell: bash
        run: |
          TOOL_CACHE="${RUNNER_TEMP}/_tool_cache"
          mkdir -p "$TOOL_CACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOL_CACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOL_CACHE" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Update submodule
        run: git submodule update --init Neuro-Dream-Hand || true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        env:
          AGENT_TOOLSDIRECTORY: ${{ runner.temp }}/_tool_cache
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          if [ "$RUNNER_OS" = "Windows" ]; then
            where python || true
            where pytest || true
          else
            which python || true
            which pytest || true
          fi
      - name: Install dependencies
        run: |
          if [ -d "Neuro-Dream-Hand" ] && [ -f "Neuro-Dream-Hand/pyproject.toml" ]; then
            python -m pip install -e ./Neuro-Dream-Hand
            python -m pip install pytest ruff mypy
          else
            echo "Submodule missing, skipping"
          fi
      - name: Lint with ruff
        run: |
          if [ -d "Neuro-Dream-Hand" ] && [ -f "Neuro-Dream-Hand/pyproject.toml" ]; then
            cd Neuro-Dream-Hand
            python -m ruff check .
            python -m ruff format --check .
          fi
      - name: Type check with mypy
        run: |
          if [ -d "Neuro-Dream-Hand" ] && [ -f "Neuro-Dream-Hand/pyproject.toml" ]; then
            cd Neuro-Dream-Hand
            python -m mypy .
          fi
      - name: Run tests
        run: |
          if [ -d "Neuro-Dream-Hand" ] && [ -f "Neuro-Dream-Hand/pyproject.toml" ]; then
            cd Neuro-Dream-Hand
            python -m pytest --tb=short --hypothesis-show-statistics
          fi

  test-neurocnl-backend:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurocnl == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - name: Configure tool cache
        shell: bash
        run: |
          TOOL_CACHE="${RUNNER_TEMP}/_tool_cache"
          mkdir -p "$TOOL_CACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOL_CACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOL_CACHE" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Update required submodules
        run: |
          git -c http.https://github.com/.extraheader="AUTHORIZATION: basic ${{ secrets.GITHUB_TOKEN }}" submodule update --init neurocnl || true
          git -c http.https://github.com/.extraheader="AUTHORIZATION: basic ${{ secrets.GITHUB_TOKEN }}" submodule update --init Neuro-Dream-Hand || true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        env:
          AGENT_TOOLSDIRECTORY: ${{ runner.temp }}/_tool_cache
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          if [ "$RUNNER_OS" = "Windows" ]; then
            where python || true
            where pytest || true
          else
            which python || true
            which pytest || true
          fi
      - name: Install core package
        run: |
          if [ -d "neurocnl" ] && [ -f "neurocnl/pyproject.toml" ]; then
            python -m pip install -e ./neurocnl
            python -m pip install pytest httpx ruff mypy
          else
            echo "neurocnl missing, skipping install"
          fi
      - name: Install Neuro-Dream-Hand
        run: |
          if [ -d "Neuro-Dream-Hand" ] && [ -f "Neuro-Dream-Hand/pyproject.toml" ]; then
            python -m pip install -e ./Neuro-Dream-Hand
          else
            echo "Neuro-Dream-Hand missing, skipping install"
          fi
      - name: Lint with ruff
        run: |
          if [ -f "neurocnl/pyproject.toml" ]; then
            cd neurocnl
            python -m ruff check .
            python -m ruff format --check .
          else
            echo "neurocnl missing, skipping lint"
          fi
      - name: Type check with mypy
        run: |
          if [ -f "neurocnl/pyproject.toml" ]; then
            cd neurocnl
            python -m mypy .
          else
            echo "neurocnl missing, skipping type check"
          fi
      - name: Install backend deps
        run: |
          if [ -f "neurocnl/backend/requirements.txt" ]; then
            cd neurocnl/backend
            python -m pip install -r requirements.txt
          else
            echo "neurocnl/backend missing, skipping backend deps"
          fi
      - name: Run core tests
        run: |
          if [ -f "neurocnl/pyproject.toml" ]; then
            cd neurocnl
            python -m pytest neurocnl/ -v --tb=short
          else
            echo "neurocnl missing, skipping core tests"
          fi
      - name: Run backend tests
        run: |
          if [ -f "neurocnl/backend/requirements.txt" ]; then
            cd neurocnl/backend
            PYTHONPATH=. python -m pytest backend/tests --tb=short
          else
            echo "neurocnl/backend missing, skipping backend tests"
          fi

  test-neurobench:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurobench == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - name: Configure tool cache
        shell: bash
        run: |
          TOOL_CACHE="${RUNNER_TEMP}/_tool_cache"
          mkdir -p "$TOOL_CACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOL_CACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOL_CACHE" >> "$GITHUB_ENV"
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Update submodule
        run: git submodule update --init Neurobench || true
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        env:
          AGENT_TOOLSDIRECTORY: ${{ runner.temp }}/_tool_cache
      - name: Python diagnostics
        run: |
          python --version
          python -m pip --version
          if [ "$RUNNER_OS" = "Windows" ]; then
            where python || true
            where pytest || true
          else
            which python || true
            which pytest || true
          fi
      - name: Install poetry
        run: |
          if [ -d "Neurobench/neurobench" ] && [ -f "Neurobench/neurobench/pyproject.toml" ]; then
            python -m pip install poetry
          fi
      - name: Install dependencies
        run: |
          if [ -d "Neurobench/neurobench" ] && [ -f "Neurobench/neurobench/pyproject.toml" ]; then
            cd Neurobench/neurobench && poetry install
          fi
      - name: Lint with ruff
        run: |
          if [ -d "Neurobench/neurobench" ] && [ -f "Neurobench/neurobench/pyproject.toml" ]; then
            cd Neurobench/neurobench
            poetry run ruff check .
            poetry run ruff format --check .
          fi
      - name: Type check with mypy
        run: |
          if [ -d "Neurobench/neurobench" ] && [ -f "Neurobench/neurobench/pyproject.toml" ]; then
            cd Neurobench/neurobench
            poetry run mypy .
          fi
      - name: Run tests
        run: |
          if [ -d "Neurobench/neurobench" ] && [ -f "Neurobench/neurobench/pyproject.toml" ]; then
            cd Neurobench/neurobench
            poetry run pytest --tb=short --hypothesis-show-statistics
          fi

  test-nmtk-ui-core:
    needs: detect-changes
    if: needs.detect-changes.outputs.nmtk_ui_core == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - uses: actions/checkout@v4
        with:
          submodules: false
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false
      - name: Install dependencies
        run: |
          flutter pub get --directory nmtk_ui_core
      - name: Analyze
        run: |
          flutter analyze --directory nmtk_ui_core
      - name: Run tests
        run: |
          cd nmtk_ui_core && flutter test

  test-neurotoolkit:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurotoolkit == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - uses: actions/checkout@v4
        with:
          submodules: false
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false
      - name: Install dependencies
        run: |
          flutter pub get --directory nmtk/neuro_toolkit
      - name: Analyze
        run: |
          flutter analyze --directory nmtk/neuro_toolkit
      - name: Run tests
        run: |
          cd nmtk/neuro_toolkit && flutter test

  test-neurocnl-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurocnl_frontend == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0
      - name: Update submodules safely
        run: git submodule update --init || true
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false
      - name: Install dependencies
        run: |
          if [ -d "neurocnl/frontend" ] && [ -f "neurocnl/frontend/pubspec.yaml" ]; then
            flutter pub get --directory neurocnl/frontend
          fi
      - name: Analyze
        run: |
          if [ -d "neurocnl/frontend" ] && [ -f "neurocnl/frontend/pubspec.yaml" ]; then
            flutter analyze --directory neurocnl/frontend
          fi
      - name: Run tests
        run: |
          if [ -d "neurocnl/frontend" ] && [ -f "neurocnl/frontend/pubspec.yaml" ]; then
            cd neurocnl/frontend && flutter test
          fi

  test-neurochip-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurochip_frontend == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0
      - name: Update submodules safely
        run: git submodule update --init || true
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false
      - name: Install dependencies
        run: |
          if [ -d "Neurochip/frontend" ] && [ -f "Neurochip/frontend/pubspec.yaml" ]; then
            flutter pub get --directory Neurochip/frontend
          fi
      - name: Analyze
        run: |
          if [ -d "Neurochip/frontend" ] && [ -f "Neurochip/frontend/pubspec.yaml" ]; then
            flutter analyze --directory Neurochip/frontend
          fi

  test-neurohub-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurohub_frontend == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0
      - name: Update submodules safely
        run: git submodule update --init || true
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false
      - name: Install dependencies
        run: |
          if [ -d "Neurohub/frontend" ] && [ -f "Neurohub/frontend/pubspec.yaml" ]; then
            flutter pub get --directory Neurohub/frontend
          fi
      - name: Analyze
        run: |
          if [ -d "Neurohub/frontend" ] && [ -f "Neurohub/frontend/pubspec.yaml" ]; then
            flutter analyze --directory Neurohub/frontend
          fi

  test-neurosense-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurosense_frontend == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0
      - name: Update submodules safely
        run: git submodule update --init || true
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false
      - name: Install dependencies
        run: |
          if [ -d "Neurosense/frontend" ] && [ -f "Neurosense/frontend/pubspec.yaml" ]; then
            flutter pub get --directory Neurosense/frontend
          fi
      - name: Analyze
        run: |
          if [ -d "Neurosense/frontend" ] && [ -f "Neurosense/frontend/pubspec.yaml" ]; then
            flutter analyze --directory Neurosense/frontend
          fi

  test-neurobench-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.neurobench_frontend == 'true'
    strategy:
      fail-fast: false
      matrix:
        os:
          - [self-hosted, macOS, ARM64]
          - [self-hosted, Windows, X64]
          - [self-hosted, Linux, X64]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Add Git to PATH (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          if exist "C:\Program Files\Git\cmd" echo C:\Program Files\Git\cmd>>%GITHUB_PATH%
      - uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0
      - name: Update submodules safely
        run: git submodule update --init || true
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: false
      - name: Install dependencies
        run: |
          if [ -d "Neurobench/frontend" ] && [ -f "Neurobench/frontend/pubspec.yaml" ]; then
            flutter pub get --directory Neurobench/frontend
          fi
      - name: Analyze
        run: |
          if [ -d "Neurobench/frontend" ] && [ -f "Neurobench/frontend/pubspec.yaml" ]; then
            flutter analyze --directory Neurobench/frontend
          fi

  # Summary job that branch protection can require
  ci-passed:
    runs-on: [self-hosted, Linux, X64]
    needs:
      - pre-commit
      - test-neurosim
      - test-neurochip
      - test-neurochip-frontend
      - test-neurosense
      - test-neurosense-frontend
      - test-neurohub
      - test-neurohub-frontend
      - test-neurodreamhand
      - test-neurobench
      - test-neurocnl-backend
      - test-neurocnl-frontend
      - test-neurobench-frontend
      - test-neurotoolkit
      - test-nmtk-ui-core
    if: always()
    steps:
      - name: Check results
        shell: bash
        run: |
          echo "Results:"
          echo "  neurosim:       ${{ needs.test-neurosim.result }}"
          echo "  neurochip:      ${{ needs.test-neurochip.result }}"
          echo "  neurosense:     ${{ needs.test-neurosense.result }}"
          echo "  neurohub:       ${{ needs.test-neurohub.result }}"
          echo "  neurodreamhand: ${{ needs.test-neurodreamhand.result }}"
          echo "  neurobench:     ${{ needs.test-neurobench.result }}"
          echo "  neurocnl:       ${{ needs.test-neurocnl-backend.result }}"
          echo "  nmtk-ui-core:   ${{ needs.test-nmtk-ui-core.result }}"
          echo "  neurotoolkit:   ${{ needs.test-neurotoolkit.result }}"

          # Fail if any job failed (skipped is OK — means no changes in that module)
          for result in \
            "${{ needs.pre-commit.result }}" \
            "${{ needs.test-neurosim.result }}" \
            "${{ needs.test-neurochip.result }}" \
            "${{ needs.test-neurochip-frontend.result }}" \
            "${{ needs.test-neurosense.result }}" \
            "${{ needs.test-neurosense-frontend.result }}" \
            "${{ needs.test-neurohub.result }}" \
            "${{ needs.test-neurohub-frontend.result }}" \
            "${{ needs.test-neurodreamhand.result }}" \
            "${{ needs.test-neurobench.result }}" \
            "${{ needs.test-neurobench-frontend.result }}" \
            "${{ needs.test-neurocnl-backend.result }}" \
            "${{ needs.test-neurocnl-frontend.result }}" \
            "${{ needs.test-nmtk-ui-core.result }}" \
            "${{ needs.test-neurotoolkit.result }}"; do
            if [ "$result" = "failure" ]; then
              echo "::error::One or more test jobs failed"
              exit 1
            fi
          done
          echo "All checks passed (or were skipped)"
