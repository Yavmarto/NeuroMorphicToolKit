"""{{ project_name }} — NeuroCNL → PYNQ-Z2 deployment scaffold.

Generated by `neuro new --framework neurocnl --target pynq --task {{ task }}`.
PYNQ import is optional — must not be imported at module level.
"""

from __future__ import annotations


def main() -> None:
    print(f"Project: {{ project_name }}, task: {{ task }}")
    print("Compile your CNL spec with neurocnl, export NIR, then deploy via NeuroChip.")
    print("Run `neuro status` to check if NeuroChip backend is reachable.")


if __name__ == "__main__":
    main()
