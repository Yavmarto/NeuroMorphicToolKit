"""{{ project_name }} — NIR + Lava simulator.

Generated by `neuro new --framework nir --target lava_sim --task {{ task }}`.
"""

from __future__ import annotations


def main() -> None:
    try:
        import lava  # noqa: F401
    except ImportError:
        print("lava-nc not installed. Run `uv sync --extra lava`. Simulation skipped.")
        return

    print(f"Project: {{ project_name }}, task: {{ task }}")
    print("Connect NIR graph to Lava runtime via the NMTK simulator contract.")


if __name__ == "__main__":
    main()
