# {{ project_name }}

**Framework**: NIR → Lava simulator  
**Task**: {{ task }}

## Getting started

```bash
uv sync --extra lava
uv run python src/main.py
```

Install `lava-nc` only when available; base simulation runs without it.
