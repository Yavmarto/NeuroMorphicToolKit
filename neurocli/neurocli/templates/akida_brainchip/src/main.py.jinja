"""{{ project_name }} — Akida + BrainChip MetaTF scaffold.

Generated by `neuro new --framework akida --target brainchip --task {{ task }}`.
Akida SDK is an optional import — never imported at module level.
"""

from __future__ import annotations


def main() -> None:
    try:
        import akida  # type: ignore[import]  # noqa: F401
    except ImportError:
        print("Akida SDK not installed (requires Linux + MetaTF). Scaffold only.")
        return

    print(f"Project: {{ project_name }}, task: {{ task }}")
    print("Compile NIR → Akida model via cnn2snn, then deploy to BrainChip hardware.")


if __name__ == "__main__":
    main()
