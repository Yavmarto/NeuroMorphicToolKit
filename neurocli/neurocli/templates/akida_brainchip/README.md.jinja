# {{ project_name }}

**Framework**: Akida + BrainChip MetaTF
**Task**: {{ task }}

## Getting started

```bash
uv sync --extra akida   # requires Linux + MetaTF SDK
uv run python src/main.py
```

Akida runtime is an optional dependency — base scaffold imports are clean.
