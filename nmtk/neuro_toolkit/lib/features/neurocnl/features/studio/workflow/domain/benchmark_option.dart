/// Immutable setup benchmark choice.
class BenchmarkOption {
  const BenchmarkOption({
    required this.id,
    required this.label,
    required this.subtitle,
  });

  final String id;
  final String label;
  final String subtitle;
}
