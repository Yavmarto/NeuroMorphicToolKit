"""neurosim domain.

The merged neurosim package now ships from the `neurocnl/` checkout. Its
routers carry their own /api/neurosim/ prefix, so no additional prefix is
needed when mounting in suite_api.
"""
