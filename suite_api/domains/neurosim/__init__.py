"""neurosim domain.

The neurosim package is installed as a top-level package (pip install -e Neurosim/).
Its routers carry their own /api/neurosim/ prefix, so no additional prefix is needed
when mounting in suite_api.
"""
