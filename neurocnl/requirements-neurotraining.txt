# Requirements for the Neurotraining Factory Mission (Phase 1-4).
# Split into layers so you can install only what you need.
#
# Full install:      pip install -r requirements-neurotraining.txt
# Library-only:      pip install -r requirements-neurotraining.txt --only-binary :all: -r <(grep "^#= LIBRARY" requirements-neurotraining.txt)
# Backend-only:      pip install -r requirements-neurotraining.txt --only-binary :all: -r <(grep "^#= BACKEND" requirements-neurotraining.txt)

#= CORE — always required
pytest>=7.0.0
pytest-cov
ruff
mypy

#= LIBRARY — neurocnl/neurocnl/ tests (requires full package install)
nengo>=3.2.0
numpy>=2.5.3
nir>=1.0.0
structlog>=24.1.0

#= BACKEND — neurocnl/backend/ tests
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
sse-starlette>=1.8.0
slowapi>=0.1.9
aiosqlite>=0.20.0
prometheus-client>=0.19.0

#= OPTIONAL — sleep_pes adapter runtime (not required for tests)
# neurodreamhand>=0.1.0

#= OPTIONAL — snnTorch adapter runtime (not required for core tests)
# torch>=1.8.0
# snntorch>=0.9.0

#= DEV — linting, formatting
pre-commit
black
isort
