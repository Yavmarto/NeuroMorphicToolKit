[project]
name = "neurocnl-physics-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
]

[project.optional-dependencies]
mujoco = ["mujoco>=3.0"]
