/// Barrel for the Neat Events page widgets.
///
/// Each widget lives in its own file. This file is preserved only for
/// backwards-compatible imports (e.g. `import 'cut/event_widgets.dart';`).
library;

export 'event_card.dart';
